<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="description" content="Web site created using create-snowpack-app" />
  <style>
    body {
      font-family: 'PT Serif', serif;
      margin: 0;
      padding: 0;
    }

    /* Style the labels */
    label {
      display: block;
      margin-bottom: 5px;
      font-weight: bold;
    }

    /* Style the inputs */
    input {
      padding: 8px;
      margin-bottom: 15px;
      width: 100%;
      border: 1px solid #ccc;
      border-radius: 4px;
    }

    /* Style the submit button */
    input[type="submit"] {
      background-color: #4CAF50;
      color: white;
      padding: 12px 20px;
      border: none;
      border-radius: 4px;
      cursor: pointer;
    }

    /* Style the form message */
    .line {
      margin: 0;
      color: red;
    }

    /* Style the advice section */
    .div3 {
      margin-top: 20px;
      border: 1px solid #ccc;
      background-color: #f2f2f2;
      padding: 10px;
    }

    /* Style the advice section text */
    .div3 p {
      margin: 0;
      color: red;
    }

    /* Style the section */
    .sec3 {
      width: 50%;
      margin: auto;
      padding: 20px;
      border: 1px solid #ccc;
      border-radius: 4px;
    }

    /* Style the main section */
    .sec2 {
      padding: 50px 0;
    }

    /* Style the header */
    header {
      color: #fff;
      padding: 10px;
    }

    /* Style the header link */
    header a {
      color: #fff;
      text-decoration: none;
    }

    /* Style the header link when hover */
    header a:hover {
      text-decoration: underline;
    }

    /* Style the header h1 */
    header h1 {
      margin: 0;
    }

    /* Style the header h1 a */
    header h1 a {
      color: #fff;
    }

    .header {
      background-color: #333;
      color: white;
      padding: 10px;
    }

    .items {
      display: flex;
      justify-content: space-between;
      align-items: center;
      max-width: 800px;
      margin: 0 auto;
    }

    .items a {
      color: white;
      text-decoration: none;
      font-weight: bold;
      font-size: 1.2rem;
      padding: 10px;
      border-radius: 5px;
    }

    .items a:hover {
      background-color: rgba(255, 255, 255, 0.1);
    }
  </style>
  <title>Sign-up</title>
</head>

<body>
  <div class="header">
    <div class="items">
      <a href="/sign-in">Sign-in</a>
      <a href="/sign-up">Sign-up</a>
      <a href="/">Home</a>
    </div>
  </div> <% if (message.length> 0) { %>
  <div class="div2">
    <p class="line">
      <%= message %>
    </p>
  </div>
  <% } %>
  <main class="sec2">
    <section class="sec3">
      <h2>Registration:</h2>
      <form action="/sign-up" method="POST">
        <label for="name">Username:</label><br>
        <input type="text" id="name" name="name" placeholder="username" required><br>
        <label for="email">E-mail:</label><br>
        <input type="email" id="email" name="email" placeholder="xxxx@xxx.com" required><br>
        <label for="password">Password:</label><br>
        <input type="password" id="password" name="password" autocomplete="on" placeholder="password" required><br>
        <input type="hidden" name="_csrf" value="<%= csrfTocken %>"><br>
        <input type="submit" value="Register"><br>
        <div class="div3">
          <p>Please note that the username must consist of letters only. Moreover, the password must meet the following requirements: it must be at least 8 characters in length, and it must contain at least one uppercase letter, one lowercase letter, and one digit.</p>
        </div>
      </form>
    </section>
  </main>

</body>

</html>
<!-- This the html for the form.html.-->